export enum ModalType {
  login,
  registration,
  passwordReset,
  passwordRecovery,
  registrationSuccess,
  fortune = 9
}

export enum ProfileModalType {
  paymentHistory = 5,
  profile = 6,
  settings = 7,
  wallet = 8
}

export enum CookiesType {
  accessToken = 'accessToken',
}
