export enum ModalType {
  login,
  registration,
  passwordReset,
  passwordRecovery,
  registrationSuccess,
  fortune = 9,
  bonus = 10
}

export enum ProfileModalType {
  paymentHistory = 5,
  profile = 6,
  settings = 7,
  wallet = 8
}

export enum CookiesType {
  accessToken = 'accessToken',
}
