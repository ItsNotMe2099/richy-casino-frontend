export enum ModalType {
  login,
  registration,
  passwordReset,
  passwordRecovery,
  registrationSuccess
}

export enum ProfileModalType {
  paymentHistory
}

export enum CookiesType {
  accessToken = 'accessToken',
}
